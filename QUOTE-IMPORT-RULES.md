# QUOTE-IMPORT-RULES.md — กติกานำเข้ารถจากเอกสาร (ใบ PI / Proforma / Sales Contract)

> **อัปเดต:** 12 ก.ย. 2569 · โค้ด: [lib/quoteImport/](lib/quoteImport/) · หน้าจอ: [components/QuoteImport.tsx](components/QuoteImport.tsx)
> เกิดจากบั๊กจริง: ใบ **HCTH-BFL2026091402** (CBD15-WS 4 คัน + CBD20-WS 6 คัน) ถูกอ่านเป็น **CBD15-WS 6 คัน** แล้วบันทึกลงสต็อกเงียบๆ

---

## กติกาข้อ 1 — **1 รุ่น = 1 บล็อกในเอกสาร** (ห้ามเอารุ่นแรกไปใส่ทุกคัน)

เอกสารผู้ผลิตเรียงเป็นบล็อก: `รุ่น → สเปก → ราคาต่อหน่วย → จำนวน → Serial No. → SUBTOTAL → VAT → TOTAL`
**parser ต้องตัดเอกสารเป็นบล็อกละรุ่น แล้วอ่าน SN/จำนวน/ราคา จากบล็อกของรุ่นนั้นเท่านั้น**

| ห้าม | ต้อง |
|---|---|
| `build(firstModel, sn, ...)` กับทุกคัน | `build(modelOfThisBlock, sn, ...)` |
| หาจำนวน/ราคาจากทั้งเอกสาร | หาในบล็อกของรุ่นนั้น |
| เดาเมื่อ SN ไม่ครบ | ติดธงให้คนตรวจ |

> ต้นเหตุบั๊กเดิม: `hangcha.ts` format A/B ใช้ `firstModel` กับรถทุกคัน — ใบ 2 รุ่นจึงกลายเป็นรุ่นเดียว

## กติกาข้อ 2 — **ต้นทุนต่อคัน = SUBTOTAL (ก่อน VAT) ÷ จำนวนที่เอกสารระบุ**

หารด้วย **จำนวนตามเอกสาร** เสมอ ไม่ใช่จำนวน SN ที่อ่านได้
(SN มาไม่ครบ → หารด้วยจำนวน SN จะได้ราคาเพี้ยน เช่น 70,000 ÷ 3 = 23,333 แทนที่จะเป็น 17,500)

## กติกาข้อ 3 — **ทุกครั้งที่นำเข้า ต้องเทียบจำนวนกับเอกสารก่อนบันทึก** ⭐

parser ต้องคืน `docCheck` = ตัวเลขที่**เอกสารบอกเอง** (ไม่ใช่ตัวเลขที่ parser นับได้)

| เจ้า | อ่านจำนวนจาก |
|---|---|
| HANGCHA | แถว `SUBTOTAL 小计 : <จำนวน> <ยอด>` ต่อรุ่น + `TOTAL AMOUNT 总计 : <รวม> <ยอดรวม>` ท้ายใบ |
| HELI | คอลัมน์ QUANTITY (เลขระหว่าง `THB <ต่อหน่วย>` กับ `THB <ยอดรวม>`) ต่อรายการ |
| STAXX / ROCKMAN / EP | ยังไม่มี → หน้าจอขึ้น "ตรวจเอง" ให้คนนับกับใบจริง |

หน้าตรวจทานแสดงตาราง **รุ่น · เอกสารระบุ · ระบบอ่านได้ · ผล** ทุกครั้ง
- ตรงกัน → แถบเขียว บันทึกได้
- **ไม่ตรง → แถบแดง ปุ่มบันทึกถูกล็อก** ต้องแก้จำนวนให้ตรง หรือติ๊ก "ตรวจกับใบจริงแล้ว" ก่อนถึงจะบันทึกได้

## กติกาข้อ 4 — **ไม่ชัวร์ ให้ติดธง ห้ามเดา**

ติดธงบนการ์ดให้คนตัดสิน: `ไม่พบ SN` · `ไม่พบราคาทุน` · `เอกสารระบุ N คัน แต่พบ SN M ตัว` · `อ่านจำนวนจากลำดับแถวราคา`
ยกเว้น **รถสั่งผลิต (KD)** ที่ไม่มี SN = ปกติ ไม่ต้องเตือน (ดู [SN-RULES.md](SN-RULES.md) ข้อ 5.ข)

---

## เช็กลิสต์ก่อนกดบันทึก (ทีมงาน)

1. ☐ แถบ **"ตรวจจำนวนกับเอกสาร"** เป็นสีเขียว — ถ้าแดงห้ามบันทึก
2. ☐ นับจำนวนรุ่นในตารางเทียบกับใบจริงด้วยตา (โดยเฉพาะใบที่มีหลายรุ่น)
3. ☐ ราคาทุนต่อคันตรงกับคอลัมน์ UNIT PRICE ในใบ
4. ☐ กรอก **"วันสั่งซื้อรถ"** ให้ตรงวันที่ในเอกสาร (รถ KD ใช้คำนวณวันคาดได้ SN)
5. ☐ อ่านธงเตือนสีเหลืองบนการ์ดให้ครบก่อนกดบันทึก

## เช็กลิสต์ก่อนแก้ parser (คนเขียนโค้ด)

1. ☐ ตัดเป็นบล็อกละรุ่นแล้วหรือยัง (กติกาข้อ 1)
2. ☐ คืน `docCheck` ครบทั้ง `byModel` และ `totalQty` (กติกาข้อ 3)
3. ☐ ทดสอบด้วยใบ **2 รุ่นขึ้นไป** เสมอ — ใบรุ่นเดียวจับบั๊กประเภทนี้ไม่ได้
4. ☐ ทดสอบเคส "SN มาไม่ครบ" ว่าราคาทุนยังถูก และมีธงเตือน
