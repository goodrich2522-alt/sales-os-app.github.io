# QUOTE-IMPORT-RULES.md — กติกานำเข้ารถจากเอกสาร (ใบ PI / Proforma / Sales Contract)

> **อัปเดต:** 12 ก.ย. 2569 · โค้ด: [lib/quoteImport/](lib/quoteImport/) · หน้าจอ: [components/QuoteImport.tsx](components/QuoteImport.tsx)
> เกิดจากบั๊กจริง: ใบ **HCTH-BFL2026091402** (CBD15-WS 4 คัน + CBD20-WS 6 คัน) ถูกอ่านเป็น **CBD15-WS 6 คัน** แล้วบันทึกลงสต็อกเงียบๆ

---

## กติกาข้อ 1 — **1 รุ่น = 1 บล็อกในเอกสาร** (ห้ามเอารุ่นแรกไปใส่ทุกคัน)

เอกสารผู้ผลิตเรียงเป็นบล็อก: `รุ่น → สเปก → ราคาต่อหน่วย → จำนวน → Serial No. → SUBTOTAL → VAT → TOTAL`
**parser ต้องตัดเอกสารเป็นบล็อกละรุ่น แล้วอ่าน SN/จำนวน/ราคา จากบล็อกของรุ่นนั้นเท่านั้น**

| ห้าม | ต้อง |
|---|---|
| `build(firstModel, sn, ...)` กับทุกคัน | `build(modelOfThisBlock, sn, ...)` |
| หาจำนวน/ราคาจากทั้งเอกสาร | หาในบล็อกของรุ่นนั้น |
| เดาเมื่อ SN ไม่ครบ | ติดธงให้คนตรวจ |

> ต้นเหตุบั๊กเดิม: `hangcha.ts` format A/B ใช้ `firstModel` กับรถทุกคัน — ใบ 2 รุ่นจึงกลายเป็นรุ่นเดียว

## กติกาข้อ 2 — **ต้นทุนต่อคัน = SUBTOTAL (ก่อน VAT) ÷ จำนวนที่เอกสารระบุ**

หารด้วย **จำนวนตามเอกสาร** เสมอ ไม่ใช่จำนวน SN ที่อ่านได้
(SN มาไม่ครบ → หารด้วยจำนวน SN จะได้ราคาเพี้ยน เช่น 70,000 ÷ 3 = 23,333 แทนที่จะเป็น 17,500)

## กติกาข้อ 3 — **ทุกครั้งที่นำเข้า ต้องเทียบจำนวนกับเอกสารก่อนบันทึก** ⭐

parser ต้องคืน `docCheck` = ตัวเลขที่**เอกสารบอกเอง** (ไม่ใช่ตัวเลขที่ parser นับได้)

| เจ้า | อ่านจำนวนจาก |
|---|---|
| HANGCHA | แถว `SUBTOTAL 小计 : <จำนวน> <ยอด>` ต่อรุ่น + `TOTAL AMOUNT 总计 : <รวม> <ยอดรวม>` ท้ายใบ |
| HELI | คอลัมน์ QUANTITY (เลขระหว่าง `THB <ต่อหน่วย>` กับ `THB <ยอดรวม>`) ต่อรายการ |
| STAXX / ROCKMAN / EP | ยังไม่มี → หน้าจอขึ้น "ตรวจเอง" ให้คนนับกับใบจริง |

หน้าตรวจทานแสดงตาราง **รุ่น · เอกสารระบุ · ระบบอ่านได้ · ผล** ทุกครั้ง
- ตรงกัน → แถบเขียว บันทึกได้
- **ไม่ตรง → แถบแดง ปุ่มบันทึกถูกล็อก** ต้องแก้จำนวนให้ตรง หรือติ๊ก "ตรวจกับใบจริงแล้ว" ก่อนถึงจะบันทึกได้

## กติกาข้อ 4 — **รถที่ยังไม่มี SN ต้องมีเลข PI**

รหัสชั่วคราวคือ `<เลข PI>#<ลำดับ>` ([SN-RULES.md](SN-RULES.md) ข้อ 5.ข)
ถ้าไม่มีทั้ง SN และเลข PI รหัสจะกลายเป็น `PI#1`, `PI#2` ... ซึ่ง **ชนกันข้ามใบ**
(พบของจริงในระบบ 4 คัน: `PI#7` `PI#8` `PI#9` `PI#26` มาจากคนละ PI แต่อยู่ใน namespace เดียวกัน)
→ หน้าตรวจทาน **ล็อกปุ่มบันทึก** จนกว่าจะกรอกเลข PI ครบทุกคันที่ยังไม่มี SN

## กติกาข้อ 5 — **ระยะเวลาส่งมอบ อ่านจากเอกสารก่อนเสมอ**

ใบ HELI เขียนไว้ตรงๆ ว่า `Delivery: 75-90 days after confirm order` (ใบ KD) หรือ `3-5 days` (รถมีสต็อก)
→ ใช้ค่าจากใบคำนวณวันคาดได้ SN · ไม่มีในใบค่อยใช้ค่ากลาง 60-90 วัน (`KD_LEAD_MIN/MAX_DAYS`)

## กติกาข้อ 6 — **ไม่ชัวร์ ให้ติดธง ห้ามเดา**

ติดธงบนการ์ดให้คนตัดสิน: `ไม่พบ SN` · `ไม่พบราคาทุน` · `เอกสารระบุ N คัน แต่พบ SN M ตัว` · `อ่านจำนวนจากลำดับแถวราคา`
ยกเว้น **รถสั่งผลิต (KD)** ที่ไม่มี SN = ปกติ ไม่ต้องเตือน (ดู [SN-RULES.md](SN-RULES.md) ข้อ 5.ข)

---

## กติกาข้อ 7 — **รหัสรุ่นเก็บไว้ที่เดียว: `lib/quoteImport/models.ts`**

อาการเวลารหัสรุ่นไม่ตรง: หน้านำเข้าขึ้น **"อ่านข้อความได้แต่ไม่พบรายการรถ"** ทั้งที่ไฟล์อ่านออกปกติ

**รูปแบบรหัสรุ่น:** `<คำนำหน้า><อักษรประเภทย่อย 0-2 ตัว><พิกัดยก><ส่วนขยาย>`

| ตัวอย่าง | คำนำหน้า | ประเภทย่อย | พิกัด |
|---|---|---|---|
| `CPCD30-Q22K2` | CPCD | — | 30 → 3.0 ตัน |
| `CBD20J-LI-S` | CBD | — | 20 → 2.0 ตัน |
| **`CQDM15-810`** | CQD | **M** | 15 → 1.5 ตัน |

> ⚠️ **ตัวอักษรหลังคำนำหน้าคือ "ประเภทย่อย" ไม่ใช่ความผิดพลาด**
> `CQD` = Reach Truck (นั่งขับ) · `CQDM` = Reach Stacker มีแท่นยืน+แขนจับ — **คนละประเภทกัน**
> (12 ก.ย. 2569 — ใบ C20726201-132 อ่านไม่ออกเพราะ regex บังคับให้ตัวเลขตามคำนำหน้าทันที)

**เจอรุ่นใหม่ที่อ่านไม่ออก:**
1. เพิ่มคำนำหน้าใน `MODEL_PREFIXES` (เรียง **ยาวก่อนสั้น** — `CPCD` ต้องมาก่อน `CPD`)
2. เขียนความหมายของประเภทย่อยไว้ในคอมเมนต์ — ไฟล์นี้คือ "ความจำ" ของแอป
3. เพิ่มใบนั้นเข้า `local-data/quote-samples/` + `quote-expected.json` แล้วรันเทสต์

## กติกาข้อ 8 — **เรียงข้อความตามตำแหน่งบนหน้ากระดาษก่อนเสมอ**

⚠️ **ลำดับข้อความที่ฝังใน PDF ไม่ใช่ลำดับที่ตาเห็น**

ใบ `HCTH-BFL2026091402` ของจริงฝังข้อความมาแบบนี้:
```
... QUANTITY / TOTAL AMOUNT (หัวตาราง)
4 70,000.00 · 7% 4,900.00 · 74,900.00       ← ตัวเลขทั้งคอลัมน์มาก่อน
6 147,000.00 · 7% 10,290.00 · 157,290.00
10 232,190.00
... (ข้อมูลธนาคาร / หมายเหตุ / เงื่อนไข) ...
CBD20-WS 2.0ton ...     ← ชื่อรุ่นอยู่ท้ายไฟล์ และ **สลับลำดับกับที่พิมพ์บนใบ**
CBD15-WS 1.5ton ...
```
→ parser จับรุ่นคู่กับจำนวนผิดหมด = **ต้นตอที่แท้จริง**ของบั๊ก "อ่านใบผิดรุ่น/ผิดจำนวน"

**วิธีแก้:** [pdfText.ts](lib/quoteImport/pdfText.ts) `orderTextItems()` จัดเรียงใหม่ตามพิกัดจาก pdfjs
(`transform[4]` = แกน X · `transform[5]` = แกน Y) — บนลงล่าง แล้วซ้ายไปขวา ให้เหมือนที่คนอ่าน

**ตาข่ายนิรภัยชั้นที่ 2** (เผื่อจัดเรียงแล้วยังเพี้ยน): ถ้าจับคู่รุ่น↔จำนวนไม่ได้ หรือได้ไม่ครบตามที่ใบบอก
- ติดธง **"ลำดับข้อมูลในไฟล์สลับ"** ทุกคัน
- ยังรู้ยอดจริงจากใบด้วยการอ่านคอลัมน์ตัวเลขแล้ว**บวกตรวจสอบตัวเอง** — (4, 70,000) (6, 147,000) (10, 232,190) → 4+6 = 10 ✓
- ตารางตรวจจึงขึ้นแดง "เอกสารระบุ 10 · อ่านได้ 2" → **ล็อกปุ่มบันทึก** ไม่ให้ข้อมูลผิดหลุดเข้าสต็อก

> ห้ามแก้ `readPdfText` ให้กลับไปต่อข้อความตามลำดับดิบเด็ดขาด · เคสนี้อยู่ในชุดทดสอบแล้ว
> (`local-data/quote-samples/HCTH-BFL2026091402-สลับคอลัมน์.txt`)

## กติกาข้อ 9 — **ราคาต่อคันต้องมาจากแถวราคาที่ "คูณแล้วลงตัว" ไม่ใช่ THB ตัวแรก**

ใบ HELI เขียนราคาไว้ 3 ช่องติดกัน:
```
Unit price  THB 222,000   unit promo  THB 1,000   Final price  THB 221,000   QT'Y 5   IN TOTAL THB 1,105,000
```
- `THB` ตัวแรก = **ราคาป้าย** (ยังไม่หักส่วนลด) → ใช้ไม่ได้
- ที่ถูกคือ **Final price 221,000** ยืนยันด้วยการคูณ: `221,000 × 5 = 1,105,000` ✓

→ [heli.ts](lib/quoteImport/heli.ts) `qtyRow()` จับ `THB <ราคาสุทธิ> <จำนวน> THB <ยอดรวม>`
แล้วรับค่าเฉพาะเมื่อ **ราคา × จำนวน = ยอดรวม** (คลาด ≤ 0.5%) · ไม่ผ่านค่อยถอยไปใช้ THB ตัวแรก

## กติกาข้อ 10 — **ใบมีรุ่นเดียว ห้ามตัดช่วงที่ตำแหน่งชื่อรุ่น**

⚠️ ช่องในตาราง PDF **จัดกึ่งกลางแนวตั้ง** พอเรียงข้อความตามตำแหน่งจริง (กติกาข้อ 8)
ข้อมูลแถวเดียวกันจะกระจายคนละบรรทัด และบางช่องไปอยู่ **ก่อน** ชื่อรุ่น

ใบ `C20726201-134` (18 ก.ย. 2569) ของจริงออกมาแบบนี้:
```
010253T6787                                        ← SN อยู่เหนือชื่อรุ่น
010253T6789  THB 222,000 THB 1,000 THB 221,000 5 THB 1,105,000   ← แถวราคาก็อยู่เหนือ
1 CPCD25-Q22K2 ... 010253T6806                     ← ชื่อรุ่นอยู่บรรทัดกลาง
010253T6807
010253T6808
```
ตัดตั้งแต่ชื่อรุ่น → **อ่านได้ 3 จาก 5 คัน** และไปหยิบยอดรวม **1,105,000 มาเป็นราคาต่อคัน**

**วิธีแก้:** ใบที่พบรุ่นเดียว → ใช้ **ทั้งใบ** เป็นช่วงของรายการนั้น (ไม่ต้องตัด)
ใบหลายรุ่นยังตัดตามชื่อรุ่นเหมือนเดิม · เคสนี้อยู่ในชุดทดสอบแล้ว
(`local-data/quote-samples/HELI-C20726201-134-ช่องกึ่งกลาง.txt`)

## กติกาข้อ 11 — **PDF บางใบฝังข้อความ "ทีละตัวอักษร" ต้องต่อคำตามระยะห่างจริง** ⭐

อาการที่เจอ (18 ก.ย. 2569 · ใบ EP `SALES LIST-0002`): หน้าจอขึ้น **"เดาผู้ผลิตไม่ได้ · อ่านได้ unknown"**
ทั้งที่เปิดไฟล์ดูด้วยตาก็อ่านออกปกติ

สาเหตุ: pdfjs ส่งข้อความกลับมาเป็นชิ้นละ 1 ตัวอักษร (หน้าเดียว **1,739 ชิ้น**)
เดิม `orderTextItems()` ต่อชิ้นด้วยช่องว่าง (`join(" ")`) ผลลัพธ์จึงเป็น

```
E P   D i s t r i b u t i o n   ( T h a i l a n d )   C o . ,   L t d .
```
→ ไม่ตรงกับ regex ใดเลย: เดาผู้ผลิตไม่ออก อ่านรุ่น/ราคา/จำนวนไม่ได้สักตัว

**วิธีแก้:** [pdfText.ts](lib/quoteImport/pdfText.ts) `joinLine()` ต่อชิ้นข้อความ **ตามระยะห่างจริง**
- ชิ้นชิดกัน (ห่าง ≤ 0.25 × ความสูงตัวอักษร) = ตัวอักษรในคำเดียวกัน → ต่อตรงๆ
- ห่างกว่านั้น = คนละคำ/คนละช่องตาราง → ใส่ช่องว่าง 1 ตัว
- เก็บชิ้นที่เป็น "ช่องว่างล้วน" ไว้ด้วย (ใบแบบนี้ช่องว่างระหว่างคำเป็นชิ้นของมันเอง)

เท่ากับอ่านตามที่ **ตาเห็นบนกระดาษ** · ใบที่ฝังมาเป็นคำอยู่แล้วผลลัพธ์ไม่เปลี่ยน
(ทดสอบกับใบ HELI จริง 103 ใบ — ไม่มีใบไหนแย่ลง)

> ห้ามกลับไปใช้ `join(" ")` เด็ดขาด · เคสนี้อยู่ในชุดทดสอบแล้ว (`ep-GR260002`, `ep-GR260003`)

## กติกาข้อ 12 — **ตารางหลายรายการ: แบ่งตาม "แถว" แล้วแบ่ง SN ตามจำนวนในเอกสาร** ⭐

ใบ `C20726201-134` ของจริงมี **10 คัน 2 รายการ** (M400 5 คัน @221,000 + M300 5 คัน @216,000)
แต่ระบบเดิมอ่านได้แค่ **5 คัน** — รายการที่ 2 หายไปทั้งรายการ

วิธีอ่านตอนนี้ ([heli.ts](lib/quoteImport/heli.ts)):
1. **แถวรายการ** = ทุกบรรทัดที่มีชื่อรุ่น (ราคา/จำนวนอาจอยู่คนละบรรทัด)
2. ทุกบรรทัดไปอยู่กับแถวที่ **ใกล้ที่สุด** (ไม่เกิน 6 บรรทัด = ความสูงของช่อง)
   ระยะเท่ากัน → ยกให้แถวล่าง **เฉพาะเมื่อแถวบนมีราคาของตัวเองครบแล้ว**
3. ราคา/จำนวนของแถว หาตามลำดับ: บรรทัดรุ่น → ช่องรอบๆ → จำนวนท้ายบรรทัดรุ่น
   (ทุกทางต้อง **คูณกลับให้ตรง**: ราคา × จำนวน = ยอดรายการ)
4. **SN แบ่งตามจำนวนในเอกสาร** เมื่อ SN ทั้งใบรวมกันเท่ากับจำนวนรวมที่เอกสารระบุ
   เพราะ SN เรียงตามลำดับรายการเสมอ — แม่นกว่าแบ่งตามระยะบรรทัด
   (ใบ `034` ควรได้ 2/5/3 · แบ่งตามระยะบรรทัดได้ 3/3/4 ผิด)

ผลกับใบ HELI จริงทั้งคลัง 103 ใบ (วัดด้วย "จำนวนที่อ่านได้ = จำนวนที่เอกสารระบุเอง"):

| | ตรง | ไม่ตรง (ติดธงเตือน) | ไม่รู้ยอดจากใบ |
|---|---|---|---|
| เดิม | 12 | 16 | 75 |
| ตอนนี้ | **100** | 3 | 0 |

3 ใบที่ยังไม่ตรงคือใบที่ **เอกสารขัดแย้งกันเอง** (แถวบอก 6 คัน แต่ยอดท้ายใบมีรายการเดียว)
→ ระบบติดธง + ล็อกปุ่มบันทึกให้คนตรวจ ไม่เดาแทน

> กติกาข้อ 10 (ใบรุ่นเดียวใช้ทั้งใบเป็นช่วง) ยังอยู่ — แต่ตอนนี้เป็น **ทางสำรอง**
> ใช้เฉพาะใบที่เรียงบรรทัดไม่ได้ (ข้อความสลับ/สแกน)

## กติกาข้อ 13 — **EP: 1 ใบหลายรายการ · รุ่นเดียวกันซ้ำได้ ห้ามยุบ**

ใบ EP แบบ `SALES LIST` (หัวใบ `P.I No.: GR260002` · `Date: 2026/8/28`):

```
No. Model                              COMMODITY & SPECIFICATION   Q'ty   THB price     Total
1   3T Diesel Forklift/CPCD30T2        Capacity:3000kgs,             3    ฿283,000.00   ฿849,000.00
5   1.5T Electric Stacker/DS3          Capacity: 1500kg,             5    ฿42,800.00    ฿214,000.00
        Mast: 2-standard mast 2.5M
6   1.5T Electric Stacker/DS3          Capacity: 1500kg,             3    ฿42,800.00    ฿128,400.00
        Mast: 2-standard mast 3M
```

- รุ่นอยู่หลัง `/` (มีช่องว่างคั่นได้ เช่น `Reach Truck/ CQD18S2-20`)
- **รายการ 5 กับ 6 เป็นรุ่นเดียวกัน (DS3) แต่คนละเสา** → นับเป็น 2 รายการ แตกเป็น 5 + 3 คัน
- เลข PI ของ EP = `P.I No.` (ใบเก่าใช้ `Quote No. EPZL...` ยังอ่านได้ผ่านทางสำรอง)
- ตรวจยอด: ผลรวมทุกรายการต้องเท่ากับ **`Total Cost` − `7% VAT`** ไม่ตรง → ติดธงเตือนทุกคัน
  (ใบ `0002` ฉบับ 24 ส.ค. ยอดท้ายใบไม่อัปเดตตามรายการที่เพิ่ม — ฉบับยืนยันคือ 28 ส.ค.)
- ใบ EP ไม่มี SN (ยังไม่ผลิต) → ต้องมีเลข PI ตามกติกาข้อ 4

## กติกาข้อ 14 — **ใบกำกับภาษี HELI (ไฟล์สแกน) อ่านได้แล้ว** ⭐

ใบ `ORIGINAL RECEIPT/TAX INVOICE` ของ HELI = ของที่ส่งจริง + เรียกเงินจริง → **มี SN ครบทุกคัน**
และราคาที่โชว์คือราคาที่เรียกเก็บจริง (ต่างจากใบ Sales Contract ที่อาจมีส่วนลด/แก้ทีหลัง)

```
MODEL          SERIAL NUMBER  DESCRIPTION                        QTY UNIT UNIT PRICE  TOTAL AMOUNT
CPCD30-Q22K2   010303S9994    K2 3t Diesel ,QC490(high fan),G II  1  Unit  240,000.00   240,000.00
CPD15-A7LIH4-S 05015DU6326    H4 1.5t LIB Forklift,               1  Unit  273,300.00   273,300.00
```

- หัวใบ: `Invoice No.` · `Date` · **`Remark` = เลขสัญญา** (`C20726201-080`) → เลข PI ในแอป = **PI080**
- **ใบนี้มักเป็นไฟล์สแกน (ไม่มี text layer)** — แอป OCR ให้เองก่อนส่งเข้าตัวอ่าน
  ตัวอ่านจึงไม่ยึดตำแหน่งคอลัมน์ แต่ยึด "แถวที่มีทั้งรุ่นและ SN" + ตัวเลขที่คูณกลับแล้วตรง
- ตรวจยอด: ผลรวมทุกรายการต้องเท่ากับ `Sub Total` (ก่อน VAT) ไม่ตรง → ติดธงเตือนทุกคัน
  (ไฟล์สแกนอ่านตัวเลขเพี้ยนได้ จึงต้องมีด่านนี้)
- พลังงานอ่านจากคำบรรยาย: `Diesel` → ดีเซล · `LIB` (Lithium Battery) → ไฟฟ้า

โค้ด: [lib/quoteImport/heliTax.ts](lib/quoteImport/heliTax.ts) · เคสนี้อยู่ในชุดทดสอบแล้ว (`HELI-TAX-26050457`)

## กติกาข้อ 15 — **เอกสารเติมข้อมูลที่ขาดให้รถเดิมได้ แต่ห้ามทับของเดิม**

รถที่ SN ซ้ำกับในสต็อกจะไม่ถูกสร้างแถวใหม่ (กันรถผี) แต่เดิม**ข้ามทั้งแถว**
ทำให้รถที่ทุนเป็น 0 อยู่ ไม่เคยได้ทุนจากใบกำกับภาษีที่มีข้อมูลครบ

ตอนนี้หน้านำเข้าจะโชว์ **"เติมข้อมูลที่ขาดให้รถที่มีอยู่แล้ว N คัน"** พร้อมรายการทีละคันก่อนกดบันทึก

| เติมได้ | เงื่อนไข |
|---|---|
| **ราคาทุน** | **เฉพาะใบกำกับภาษี** และเฉพาะคันที่ทุนยังเป็น 0 |
| เลขที่ใบกำกับภาษี · รหัสอ้างอิงนำเข้า · เลขที่ PI | ช่องนั้นยังว่าง |
| วันรับรถ | ช่องนั้นยังว่าง |
| พิกัดยก · ยกสูง · พลังงาน · เสา (MAST) · Valve · ความยาวงา | ช่องนั้นยังว่าง |

**ห้ามทับค่าที่มีอยู่แล้วทุกกรณี** — ของเดิมอาจถูกคนแก้ด้วยมือมาแล้ว
**ราคาทุนจากใบเสนอราคา/PI ไม่นับ** เพราะเป็นราคาตั้ง ต่อรอง/แก้ทีหลังได้ (กติกา "ไม่เดาทุน")

ทุกคันที่ถูกเติมจะมีร่องรอยไว้ที่ `custom_fields["เติมข้อมูลจากเอกสาร"]` = วันที่ + เลขเอกสาร
และการแก้ถูกบันทึกลง audit log ตามปกติ · ติ๊กปิดได้ถ้าไม่ต้องการให้เติม

---

---

## เอกสาร 2 ชนิด — อ่านคนละแบบ

| | ใบสั่งซื้อ (Proforma / Sales Contract) | ใบกำกับภาษี (Tax Invoice) |
|---|---|---|
| บอกอะไร | **สั่งอะไรไป** (ยังไม่ได้ของ) | **ได้ของอะไรจริง** |
| parser | `hangcha.ts` · `heli.ts` | `hangchaTax.ts` |
| เลข PI | `P/I NO.` | `ใบสั่งซื้อ/Order No.` |
| SN | แถว `Serial No.` | ฝังในคำอธิบาย `S/N : a , b , c` |
| จำนวน | แถว `SUBTOTAL` | คอลัมน์ `จำนวน` (ตรวจด้วย จำนวน × ราคาต่อหน่วย = จำนวนเงิน) |
| มีเพิ่ม | ระยะเวลาส่งมอบ | **วันส่งรถ · เลขที่ใบกำกับ · ราคาที่จ่ายจริง** |

**ใบกำกับภาษีเชื่อถือได้กว่าเรื่องของที่ได้รับจริง** — ใบ PI อาจส่งไม่ครบ/ไม่ตรงได้
(เจอจริง: order `HCTH-BFL202605803` ใบกำกับมี 4 คัน แต่ระบบมี 3 คัน และ SN ผิด 1 ตัว)

### ⚠️ ใบกำกับภาษีมักเป็นไฟล์สแกน → ผ่าน OCR
`pdfOcr.ts` เรนเดอร์หน้า PDF เป็นรูปแล้ว OCR ในเครื่อง (ไทย+อังกฤษ)
**OCR อ่าน SN ผิดได้** — ของจริงเคยเจอ `M1BFS00963` กลายเป็น `M1BFS009633`
ทุกแถวจากใบกำกับจึงติดธง **"ตรวจ SN กับตัวรถอีกครั้ง"** เสมอ ห้ามบันทึกโดยไม่ดู


## เช็กลิสต์ก่อนกดบันทึก (ทีมงาน)

1. ☐ แถบ **"ตรวจจำนวนกับเอกสาร"** เป็นสีเขียว — ถ้าแดงห้ามบันทึก
2. ☐ นับจำนวนรุ่นในตารางเทียบกับใบจริงด้วยตา (โดยเฉพาะใบที่มีหลายรุ่น)
3. ☐ ราคาทุนต่อคันตรงกับคอลัมน์ UNIT PRICE ในใบ
4. ☐ กรอก **"วันสั่งซื้อรถ"** ให้ตรงวันที่ในเอกสาร (รถ KD ใช้คำนวณวันคาดได้ SN)
5. ☐ อ่านธงเตือนสีเหลืองบนการ์ดให้ครบก่อนกดบันทึก

## เช็กลิสต์ก่อนแก้ parser (คนเขียนโค้ด)

1. ☐ **รัน `node scripts/test-quote-parsers.mjs` ให้ผ่านครบทุกใบก่อน commit** ⬅️ สำคัญสุด
2. ☐ ตัดเป็นบล็อกรายการด้วยแถว SUBTOTAL แล้วหรือยัง (กติกาข้อ 1)
3. ☐ คืน `docCheck` ครบทั้ง `byModel` และ `totalQty` (กติกาข้อ 3)
4. ☐ เจอใบรูปแบบใหม่ → **เพิ่มเป็นตัวอย่างทดสอบทันที** (ดูด้านล่าง)

## ชุดทดสอบ

```bash
node scripts/test-quote-parsers.mjs     # ทดสอบกับใบจริงทุกใบที่เก็บไว้
```

| ไฟล์ | คืออะไร |
|---|---|
| `local-data/quote-samples/*.txt` | ข้อความจากใบ PI จริง (1 ไฟล์ = 1 ใบ) |
| `local-data/quote-expected.json` | ผลที่ถูกต้องของแต่ละใบ |

อยู่ใน `local-data/` เพราะมีราคาทุน (gitignored) — เครื่องที่ไม่มีไฟล์จะข้ามการทดสอบ ไม่ error
**เจอใบที่อ่านผิด → เพิ่มไฟล์ตัวอย่าง + ผลที่ถูกต้อง แล้วค่อยแก้ parser** จะได้ไม่พังซ้ำ

ครอบคลุมแล้ว 13 ใบ: ใบ 2 รุ่น · ใบรุ่นเดียวกัน 2 รายการคนละราคา · SN ขึ้นต้นตัวเลข · SN คั่นคอมมา ·
ใบ KD ไม่มี SN · ใบมี Delivery 75-90 days · ใบ HELI หลายรายการ
