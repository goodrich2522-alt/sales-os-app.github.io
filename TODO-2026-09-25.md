# งานค้าง — สรุปไว้ตอนจบวัน 25 ก.ย. 2569

> อ่านไฟล์นี้ก่อนเริ่มงานวันถัดไป · ตัวเลขตรวจจากฐานข้อมูลจริงเมื่อ 25 ก.ย. 2569 เวลา ~17:15
> สต็อกทั้งหมด 1,138 คัน · ปิดการขายแล้ว 770 คัน · พร้อมขาย 313 คัน

---

## ✅ ที่ทำเสร็จวันนี้ (ไม่ต้องทำซ้ำ)

| งาน | ผล |
|---|---|
| อ่านใบกำกับภาษี HELI (ไฟล์สแกน) | ใช้ได้ · กติกาข้อ 14 |
| นำเข้าใบขายย้อนหลัง ม.ค.–ก.ย. 2569 | **เข้าแล้ว** — "ขายแล้วไม่มีใบขาย" 729 → **33** |
| แก้รูปแบบวันรับรถ 66 คัน | **เหลือ 0** |
| SN มีช่องว่าง | 2 → **1** |
| แก้ชื่อรุ่น 05025DU4873 → CPD25-A7LIH4-S | แก้แล้ว |
| แก้ยี่ห้อ/รุ่นจากการ์ดรถได้ | เพิ่มช่องให้แล้ว |
| เตือน "สถานะไม่ตรงกับดีล" ผิด | แก้แล้ว (รวมตัวแปลงเป็น `lib/saleStatus.ts`) |
| เตือน "ติดตามการผลิต" ทั้งที่รับรถแล้ว | แก้แล้ว (ดู SN แทนรูปแบบรหัส) |
| ปุ่ม "✓ ตรวจแล้ว" + เก็บเป็นประวัติ | ใช้ได้ |

---

## 🔴 ต้องกดในแอป (Claude เขียนฐานข้อมูลไม่ได้ — RLS ปิดสิทธิ์ไว้)

### 1. ขายแล้วไม่มีใบขาย — เหลือ 33 คัน
หน้าตรวจสอบข้อมูล → หัวข้อแรก
ส่วนใหญ่น่าจะเป็นรถมือสอง/แฮนด์ลิฟท์ที่ไม่มีบิลในไฟล์บัญชี → **กด "✓ ตรวจแล้ว"** ได้เลยถ้าดูแล้วปกติ
ที่เป็นของจริงค่อยให้เซลล์เปิดดีลย้อนหลัง

### 2. รับรถเข้าคลังแล้วแต่ยังไม่ปิดการขาย — 3 ดีล
ยอดขายและค่าคอมยังไม่เข้าเดือนไหนเลย → ให้ฝ่ายขายกด "ปิดการขาย / จัดส่งแล้ว"
มีดีล `PI#4` (HELI CPD30-A6LIK2-M · SN 050309FP382) อยู่ในกลุ่มนี้ — สถานะยังค้าง "จอง/โอนมัดจำแล้ว"

### 3. SN จำนวนหลักไม่เท่าพวกเดียวกัน — 10 คัน · SN มีช่องว่าง — 1 คัน
ต้องเทียบกับ SN บนตัวรถหรือใบกำกับภาษีก่อนแก้ **ห้ามเดา**

### 4. แถวรอ SN ค้าง — 11 แถว
- 7 แถวเป็นรถสั่งผลิต (KD) → ปกติ ยังไม่ต้องทำอะไร
- **4 แถวสถานะ "รอรับ" ควรเช็กว่ารถมาถึงหรือยัง:** `PI#8` · `PI#9` (PI116KD · HELI CQD20-GB2SLI)
  และ `HCTH-BFL2026091402#8` · `#9` (HANGCHA CBD20-WS)
- `PI#8`/`PI#9` รหัสเป็น `PI#N` ซึ่งชนกันข้ามใบได้ → ตอนเติม SN จริงให้แก้ไปด้วย

### 5. SN ในบิล 79 รายการที่หารถไม่เจอ
ไฟล์: `C:\Users\Ace\Downloads\SN-หารถไม่เจอ-2569-09-25.xlsx`
- **11 รายการน่าจะพิมพ์ผิดจริง** (ส่วนใหญ่ `2`↔`Z` และ `E`↔`F`) เช่น บิล `01030423383` ↔ ระบบ `010304Z3383`
- อีก 68 รายการไม่มีรถในระบบเลย (Hand Pallet Truck 20 · Stacker/รถยกไฟฟ้า 33 · อื่นๆ 15)
  น่าจะเป็นของที่ขายโดยไม่ลงสต็อกก่อน ตามกติการถมือสอง → ถ้าใช่ ไม่ต้องทำอะไร

### 6. กรอกทุนที่ถูกล้างเป็น 0 จากบั๊ก bulk edit เก่า
PI137 และคันอื่นที่แก้ช่วงนั้น → **แก้อัตโนมัติได้ถ้าทำข้อ 8 ด้านล่าง**

### 7. รัน SQL 2 ไฟล์ (ถ้ายังไม่ได้รัน)
`supabase-rls-fix-2026-09-18.sql` · `supabase-add-admin-2026-09-24.sql`

---

## 🔵 ที่ Claude ทำต่อได้ ถ้าสั่ง

### 8. ให้ใบกำกับภาษีเติมข้อมูลที่ขาดให้แถวเดิม ⭐ แนะนำทำก่อน
ตอนนี้ถ้า SN ซ้ำกับที่มีแล้ว ระบบข้ามทั้งแถว → ทุนที่เป็น 0 ไม่ถูกเติม
ถ้าทำ: ลากใบกำกับภาษีเข้าไปแล้วมันเติม **ทุน · เลขใบกำกับ · วันรับรถ เฉพาะช่องที่ว่าง** (ไม่ทับของเดิม)
→ **ข้อ 6 หายไปเองโดยไม่ต้องกรอกมือ**

### 9. ไล่แก้รถที่ "พร้อมขาย" ทั้งที่มีใบขายอยู่แล้ว
ตัวนำเข้าข้ามกรณีนี้ (เพราะมีใบขายแล้ว) ยังไม่มีตัวไล่เก็บ

### 10. เงื่อนไขรับประกัน reach truck / reach stacker
ยังไม่ได้กำหนด — ระบบยังไม่รู้ว่าให้กี่ปี **ต้องถามผู้ใช้ ห้ามเดา**

### 11. ลดภาระหน้าสต็อก (โหลดทีละ 50 แถว · รูปโหลดช้า)
ทำถ้าหน้าค้างอีก

---

## หมายเหตุที่ต้องจำ

- **รถ 34 คันมีรหัสเป็น `PI124#3` แต่มี SN จริงแล้ว — ถูกต้องแล้ว ไม่ใช่ข้อมูลผิด**
  เพราะตอน SN มาเราเติมลงแถวเดิม ไม่สร้างแถวใหม่ (ดีล/ใบตรวจที่ผูกไว้จะได้ไม่ขาด)
  โค้ดที่จะเช็ก "รับรถหรือยัง" ต้องดู **SN** ไม่ใช่รูปแบบรหัส
- **ค่าคอม:** ดีลที่นำเข้าจากบิลภาษีไม่ถูกนับ (ตกลงกันไว้ 29 ก.ค. 2569) — ชุดที่นำเข้าวันนี้ก็ไม่นับ
- **วันรับเงิน:** ห้ามแตะ ผู้ใช้อัปโหลดไฟล์เอง
- **วันส่งมอบ = วันเริ่มประกัน**
- เขียนโค้ดผ่าน bash heredoc แล้ว `\` ในโค้ดจะหาย → regex พัง (เกิดจริงวันนี้ 2 ครั้ง)
  ใช้ Write/Edit tool หรือ `String.fromCharCode(92)` แทน แล้วอ่านไฟล์ยืนยันทุกครั้ง

## เอกสารกติกา
`STOCK-RULES.md` (ข้อ 1–10) · `QUOTE-IMPORT-RULES.md` (ข้อ 11–14) · `SALES-BACKFILL-RULES.md`

## ชุดทดสอบ
`node scripts/test-quote-parsers.mjs` (22 ใบ) · `node scripts/test-sales-import.mjs` (25 ข้อ)
