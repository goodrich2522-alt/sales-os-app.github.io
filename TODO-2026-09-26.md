# งานที่ต้องทำต่อ — 26 ก.ย. 2569

> ตัวเลขตรวจจากฐานข้อมูลจริงเมื่อ 26 ก.ย. 2569 · สต็อก 1,138 คัน · ปิดการขายแล้ว 772 · พร้อมขาย 311
> ไฟล์ก่อนหน้า: [TODO-2026-09-25.md](TODO-2026-09-25.md)

## ✅ เสร็จแล้วเช้านี้ (Claude)
- **ข้อ 7** เอกสารเติมข้อมูลที่ขาดให้รถเดิมได้ (ไม่ทับของเดิม) — กติกาข้อ 15
- **ข้อ 9** หัวข้อตรวจ "สถานะรถไม่ตรงกับใบขาย" + ปุ่มตั้งให้ตรงทั้งหมด
- **ข้อ 10** เงื่อนไขประกันรีชทรัค (เหมือนโฟล์คลิฟท์) / รีชสแตกเกอร์ (มอเตอร์+กล่องคุม 1 ปี) — กติกาสต็อกข้อ 11

---

## 🔴 ต้องกดในแอป (Claude เขียนฐานข้อมูลไม่ได้ — RLS)

### A. เก็บทุนที่เป็น 0 ด้วยใบกำกับภาษี ⭐ ทำก่อน — ของใหม่เพิ่งทำเสร็จ
หน้าสต็อก → นำเข้าข้อมูล → ลากใบกำกับภาษีของล็อตที่ทุนหาย (PI137 ฯลฯ)
จะขึ้นกล่องฟ้า "เติมข้อมูลที่ขาดให้รถที่มีอยู่แล้ว N คัน" → ตรวจรายการ → บันทึก
**ไม่ต้องกรอกทุนมือทีละคัน**

### B. กดปุ่ม ⚡ "ตั้งให้ตรงกับใบขายทั้งหมด"
หน้าตรวจสอบข้อมูล → หัวข้อ "สถานะรถไม่ตรงกับใบขาย"
รถที่ยังขึ้น "พร้อมขาย" ทั้งที่มีใบขายปิดแล้ว = **เสี่ยงขายซ้ำคัน** ควรเคลียร์วันนี้

### C. 33 คัน "ขายแล้วไม่มีใบขาย"
ส่วนใหญ่น่าจะเป็นรถมือสอง/แฮนด์ลิฟท์ที่ไม่มีบิลในไฟล์บัญชี → กด **"✓ ตรวจแล้ว"** ได้เลยถ้าปกติ
ที่เป็นของจริงให้เซลล์เปิดดีลย้อนหลัง

### D. 3 ดีล "รับรถเข้าคลังแล้วแต่ยังไม่ปิดการขาย"
ยอดขาย/ค่าคอมยังไม่เข้าเดือนไหนเลย · รวมดีล `PI#4` (HELI CPD30-A6LIK2-M · SN 050309FP382)
ที่สถานะยังค้าง "จอง/โอนมัดจำแล้ว" ทั้งที่รับรถแล้ว

### E. แก้ชื่อรุ่น `05025DU4873` — **ยังไม่ได้แก้**
ในระบบ `CPD25-M400` · ใบกำกับภาษีจริง **`CPD25-A7LIH4-S`** (M400 คือความสูงเสา ไม่ใช่ชื่อรุ่น)
เปิดการ์ดรถ → ✎ แก้ไข → ช่อง "รุ่น" (เพิ่งเพิ่มให้เมื่อวาน) → ใบขายจะถูกซิงก์ตามให้เอง

### F. SN ที่ต้องเทียบกับตัวรถ — **ห้ามเดา**
- 1 คัน SN มีช่องว่างเกิน
- 10 คัน SN จำนวนหลักไม่เท่าพวกเดียวกัน
- 11 SN ในบิลที่น่าจะพิมพ์ผิด (ไฟล์ `Downloads\SN-หารถไม่เจอ-2569-09-25.xlsx` · ส่วนใหญ่ `2`↔`Z`, `E`↔`F`)

### G. 4 แถว "รอรับ" ค้าง — เช็กว่ารถมาถึงหรือยัง
`PI#8` · `PI#9` (PI116KD · HELI CQD20-GB2SLI) — รหัส `PI#N` ชนกันข้ามใบได้ แก้ตอนเติม SN จริง
`HCTH-BFL2026091402#8` · `#9` (HANGCHA CBD20-WS)
(อีก 7 แถวเป็นรถสั่งผลิต KD — ปกติ ไม่ต้องทำอะไร)

### H. รัน SQL 2 ไฟล์ (ถ้ายังไม่ได้รัน)
`supabase-rls-fix-2026-09-18.sql` · `supabase-add-admin-2026-09-24.sql`

---

## 🔵 Claude ทำต่อได้ ถ้าสั่ง
- **ลดภาระหน้าสต็อก** (โหลดทีละ 50 แถว · รูปโหลดช้า) — ทำเฉพาะถ้าหน้ายังค้าง
- ยังไม่มีงานค้างอื่นในฝั่งโค้ด

---

## กติกาที่ต้องจำ
- รถ 34 คันรหัสเป็น `PI124#3` แต่มี SN แล้ว = **ถูกต้อง ไม่ใช่ข้อมูลผิด** (เติม SN ลงแถวเดิม)
- ค่าคอม: ดีลที่นำเข้าจากบิลภาษีไม่ถูกนับ · วันรับเงิน: ห้ามแตะ · วันส่งมอบ = วันเริ่มประกัน
- เขียนโค้ดผ่าน bash heredoc แล้ว `\` หาย → regex พัง ใช้ Write/Edit tool แทน
